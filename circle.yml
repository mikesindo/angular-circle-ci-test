
machine:
  node:
    version: 10.1.0
  services:
   - docker

dependencies:
  post:
    - docker build -t 923903436004.dkr.ecr.us-east-1.amazonaws.com/mike-repo:$CIRCLE_SHA1 .

test:
  post:
    - docker run -d -p 8080:8080 923903436004.dkr.ecr.us-east-1.amazonaws.com/mike-repo:$CIRCLE_SHA1; sleep 10
    - curl --retry 10 --retry-delay 5ecs- http://localhost:4200 | grep "Hello World!"

deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
